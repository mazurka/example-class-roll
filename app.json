{
  "name": "Mazurka Class Roll",
  "description": "Example application implementing a class roll",
  "scripts": {
    "postdeploy": "mix ecto.migrate -r ClassRoll.DB.Postgres"
  },
  "env": {
    "BUILDPACK_URL": "https://github.com/ddollar/heroku-buildpack-multi",
    "MIX_ENV": "prod"
  },
  "addons": [
    "heroku-postgresql"
  ]
}
